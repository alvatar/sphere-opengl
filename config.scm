(sphere: "opengl")
(dependencies:
 (gl-es (prelude
         (core: ffi-header))
        (load
         (core: ffi)))
 (gl-es-ext (prelude
             (core: ffi-header))
            (load
             (core: ffi)))
 (gl (prelude
      (core: ffi-header))
     (load
      (core: ffi))))

